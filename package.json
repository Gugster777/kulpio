{
  "name": "kulpio",
  "version": "3.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Kulpio — food-freshness tracker PWA. The app is a single static HTML file; this package only carries the headless smoke test.",
  "scripts": {
    "build": "node scripts/build-app.mjs",
    "pretest": "npm run build",
    "test": "node tests/structure.test.mjs && node tests/smoke.js && node tests/worker-push.test.mjs && node tests/worker-auth.test.mjs && node tests/worker-house.test.mjs && node tests/worker-ai.test.mjs",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "playwright-core": "^1.61.1"
  }
}
