{
  "name": "kulpio",
  "version": "3.0.0",
  "private": true,
  "description": "Kulpio — food-freshness tracker PWA. The app is a single static HTML file; this package only carries the headless smoke test.",
  "scripts": {
    "test": "node tests/structure.test.mjs && node tests/smoke.js && node tests/worker-push.test.mjs && node tests/worker-auth.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "playwright-core": "^1.61.1"
  }
}
